import { Notifications } from 'expo'

function notificationCallback (notif) {
	// STRING origin will be 'recieved' as soon as the notification arrives, and will be 'selected' when the user taps on the notification
	origin = notif.origin
	//JSON or maybe custom object. TBD it's up to us.
	data = data

}

newDailyNotification (head, body, data) {

}